import React from 'react';

const Loading = () => {
  return (
    <div className = "overlay">
      <h4>Loading...</h4>
      <i className = "fa fa-refresh fa-spin"/>
    </div>
  );
};

export default Loading;
